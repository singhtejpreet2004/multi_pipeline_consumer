# =============================================================================
# Plaksha Streams — CI-only Requirements
# CPU-compatible versions of the packages the test import chain needs.
# Do NOT install on the GPU server — production uses requirements.txt
# (CUDA 12.8 wheels). Versions pinned to match requirements.txt.
#
# Used by: .github/workflows/ci.yml (test job)
# Tests import consumers.consumer, which imports all of the below;
# tests/conftest.py stubs only the /data/Entry_Exit imports, not these.
# =============================================================================

numpy==2.0.2
opencv-python-headless==4.12.0.88   # headless — CI runners have no display server
torch==2.8.0                        # CPU wheel — no +cu128 suffix
torchvision==0.23.0                 # required by ultralytics; pinned to match torch
tensorflow-cpu==2.18.0              # CPU build of tensorflow==2.18.0
ultralytics==8.3.20
Flask-SocketIO==5.5.1
nvidia-ml-py3==7.352.0              # provides the pynvml module (matches requirements.txt)
kafka-python==2.2.11
